# 推理交付核对记录

日期：2026-09-13。仅核对实现等价与可运行性，不是模型成绩评估。
源模型冻结于研究仓库提交 `b741b2978165bf24baf82e2f4dc782553b8327c3` 时的
`models/tennis_event/default-fusion.json`，两专家权重均来自其绑定的正式部署副本。

## 交付内容

- 五个推理源码文件，共 717 行：`inputs.py`、`vision.py`、`dino.py`、`model.py`、`predict.py`。
- 两份事件头部署权重：全部参数张量、合同和归一化常量与原文件一致；剔除训练配置及记录。
- 一份本地 DINOv3 官方权重，原样复制，使用官方许可。
- 中文使用说明、推理单元测试和独立 Git 仓库。
- 原研究仓库仅用于读取与对照；没有在原仓库写代码、缓存、结果或 Git 提交。

## 已完成检查

`python -m unittest discover -s tests -v`：16 项通过，无跳过。
覆盖三份权重严格加载、网络输入/输出、区域时间卷积、无观测窗口、稀疏 CSV、
帧号与坐标边界、不规则 PTS、断轨后导数重置、静止坐标精度、归一化掩码、
时间窗等距取早帧及补零、55% 原图裁剪、阈值等号、NMS 半径及同分顺序、无事件输出。
`predict.py --help` 与 Ruff 检查通过。

从原仓库仅只读取得真实样本 `tracknet_game8_clip6`（131 帧）和 `game0_clip0`（69 帧）：

- 新程序从视频＋CSV 计算的 PTS、10 维轨迹和归一化结果，与原缓存/实现逐元素相等。
- 使用相同视觉特征，两样本的 B0 与视觉专家全部逐帧类别分数，与原推理入口逐元素相等。
- 真实全图及四裁剪的图像输入转换与原实现逐元素相等。
- DINO 骨干对真实五视图 CPU 输入，与官方固定版本逐元素相等，最大误差为 0。
- DINO 骨干对真实全图 CUDA autocast FP16 输入，与官方版本逐元素相等，最大误差为 0。

真实视频＋CSV 的完整 CLI 在 CPU、CUDA 分别运行成功；均完整解码 131 帧，
DINO 共前向 655 张图像，最终事件均为：第 6 帧 hit、第 19 帧 bounce、第 29 帧 hit，
与原默认入口保存的结果相同。没有使用 GT 或计算 F1。

| 实测链路 | 总内部耗时 | 解码＋DINO | 视频吞吐 | CUDA peak allocated |
|---|---:|---:|---:|---:|
| CPU，4 线程 | 185.36 秒 | 183.41 秒 | 0.71 帧/秒 | 不适用 |
| CUDA，共享 GPU | 14.56 秒 | 11.20 秒 | 9.00 帧/秒 | 约 396 MiB |

两次事件头 batch size 均为 512，DINO 图像 batch size 为 8；CPU 与 GPU 处于共享环境。
这里包含视频解码及视觉特征提取，不能与原项目只读取缓存的吞吐横比，也不是其他机器的速度保证。
原始 JSON 保存在本机 `verification/raw-cpu.json` 和 `verification/raw-cuda.json`，不提交样本结果。

独立审阅五个源码文件：未发现阻断问题，结论为 APPROVE。

## 数值与部署边界

相同输入和相同设备下的关键计算已验证逐元素等价。原默认参考结果使用以前提取的 CUDA 特征；
重新提取后，相比该历史结果，CPU 全体分数最大绝对差约 `0.004837`，CUDA 约 `0.000001707`。
本次事件帧号和类别相同，但不承诺跨设备、batch size、软件版本或不同视频的逐位一致。

没有重新训练、搜索阈值、评价全数据集或做跨平台安装实验。没有测试任意编码/损坏视频，
输入继续要求原项目支持的 MP4/MOV 与有效 packet PTS。大型视频的特征内存随帧数线性增长。

两份小模型随 Git 提交；DINO 大权重在本机齐备但不在普通 Git 历史，复制部署时必须一并携带。
该文件夹可独立移动，无原仓库运行时依赖。当前交付为本地 Git 仓库，没有创建或推送远端仓库。
